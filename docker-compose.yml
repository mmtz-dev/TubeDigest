services:
  tubedigest:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./Transcriptions:/app/Transcriptions
      - whisper-cache:/root/.cache/whisper
    environment:
      - RUNNING_IN_DOCKER=true

volumes:
  whisper-cache:
